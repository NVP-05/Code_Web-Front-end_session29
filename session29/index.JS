// Tính diện tích của một hình tam giác

// Biết chiều cao (h) = 20, cạnh đáy (a) = 30

function triangleS(a, h) {
  // function body

  let s = (a * h) / 2;

  console.log("Diện tích tam giác S =", s);
}

// Sử dụng N lần
triangleS(10, 20);
triangleS(15, 25);
